namespace tmsang.domain
{
    public class Constants
    {
        public static double LOGIN_TOKEN_EXPIRED = 60 * 24 * 1;             // 1 days expired
        public static double ACTIVE_TOKEN_EXPIRED = 60 * 24 * 1;            // 1 days expired

        public static double DISTANCE_DEFAULT = 1000;                       // 1000 m
    }
}
